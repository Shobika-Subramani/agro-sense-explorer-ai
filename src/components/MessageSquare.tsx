
import { MessageSquare as MessageSquareIcon } from "lucide-react";

const MessageSquare = (props: React.ComponentProps<typeof MessageSquareIcon>) => {
  return <MessageSquareIcon {...props} />;
};

export default MessageSquare;
